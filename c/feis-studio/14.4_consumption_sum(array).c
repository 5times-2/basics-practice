#include <stdio.h>
int main() {
	int id, prices[5] = {90, 75, 83, 89, 71};
	int toltal = 0;
	while(1) {
		scanf ("%d", &id);
		if(i == 0) {
			break;
		}
		toltal += prices[id - 1];
	}
	printf ("Toltal: %d\n", toltal);
	
	return 0;
}